import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    customIMG:{
        width:"100%"
    },
    boxBanner:{
        margin:"20px 0px"
    }
})

export default useStyles;